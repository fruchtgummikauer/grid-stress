# 03.7 — Context Appendix

Status: draft
Branch: `feature/*` off `main`
Deliverable: the final section of `notebooks/01_eda/team-EDA.ipynb` — the context appendix, the
closing Findings section, and the notebook's self-check
Depends on: [03-combined-cherry-picked-eda.md](03-combined-cherry-picked-eda.md) and
[03.1-setup.md](03.1-setup.md) (`time_series`, `YEARS`, `LOADED`), and is written last because it
closes the notebook — [03.2](03.2-sanity-check.md)–[03.6](03.6-temporal-dependence-and-extremes.md)
must all exist before this section's Findings can summarise them.

## Goal

Close `team-EDA.ipynb` with four supporting/contextual plots the team wants kept apart from the
main narrative, a "Findings" section summarising every prior 03.x section, and a self-check that
verifies the whole notebook's structural invariants without hardcoding the record's extent.

## Scope

### IN

- Four context plots (Behaviour 1–4), clearly separated as the notebook's last content section.
- The closing "Findings" section (Behaviour 5).
- The final, whole-notebook self-check (Behaviour 6).

### OUT

- Any plot already placed in [03.2](03.2-sanity-check.md)–[03.6](03.6-temporal-dependence-and-extremes.md)
  — this section holds only the four items below, plus the closing prose and self-check.
- A Pearson `SERIES` correlation heatmap — `EDA-simple-claude.ipynb` cell 66 carries no tag and
  is excluded under the project-wide rule; Behaviour 2 below is a *different* correlation view
  (Spearman, plus a wind+solar share column), not a substitute that needs the same exclusion
  reasoning restated.

## Behaviour

1. Monthly data-completeness heatmap: the joint-valid fraction of each calendar month (source:
   `EDA-hari.ipynb` cell 17, tag `optional`), rebuilt against `time_series`'s own gaps rather
   than hari's separately downloaded dataset.
2. Spearman correlation heatmap of `grid_load`, `wind_on`, `wind_off`, `solar`, `residual_load`
   and the `wind + solar / grid_load` share (source: `EDA-hari.ipynb` cell 50, tag
   `informational`).
3. Load-duration curve: `grid_load` and `residual_load` sorted descending against % of hours
   (source: `EDA-magc.ipynb` cell 26, tag `optional`), over the full record rather than magc's
   default one-year window.
4. Wind + solar share of `grid_load`, day-of-year × year heatmap (source: `EDA-hari.ipynb`
   cell 47, tag `combine with grid evolution`).
5. Close with a "Findings" section summarising, by sub-spec: the sanity-check results
   ([03.2](03.2-sanity-check.md)), the confirmed univariate and time structure
   ([03.3](03.3-univariate-and-time-structure.md)), the two-series comparisons
   ([03.4](03.4-two-series-comparison-views.md)), the calendar-structure and single-event
   findings ([03.5](03.5-calendar-structure-heatmaps.md)), the temporal-dependence, seasonal
   decomposition and residual-load/ramp findings
   ([03.6](03.6-temporal-dependence-and-extremes.md)), and the four context-appendix
   observations above.
6. Run a final, whole-notebook self-check:
   - The index is sorted and unique.
   - `time_series[SERIES]` has zero `NaN`s.
   - `time_series` carries no columns beyond `SERIES` plus the declared `DERIVED` set (the
     mechanical proof no flag or label column crept in anywhere across
     [03.2](03.2-sanity-check.md)–[03.6](03.6-temporal-dependence-and-extremes.md)).
   - Nothing mutated `time_series` since [03.1](03.1-setup.md)'s `LOADED` snapshot was taken
     (row count and index bounds match `LOADED`).
   - Do **not** assert a hardcoded row count or date bound — unlike
     `EDA-simple-claude.ipynb` cell 84's "tier 2" check, this notebook's data extent is not
     assumed fixed (see [03.1-setup.md](03.1-setup.md)).

## Data

No series beyond what [03.1](03.1-setup.md) already defines.

## Edge cases

- **This section is genuinely last.** Nothing in Behaviour 1–4 may depend on state that a later
  cell would still need to define — there is no "later" in this notebook after this section.
- **The excluded Pearson correlation heatmap.** Behaviour 2's Spearman heatmap does not fill the
  gap left by `EDA-simple-claude.ipynb` cell 66's exclusion — they use different methods and
  slightly different columns (Behaviour 2 adds the wind+solar share ratio). Do not treat
  Behaviour 2 as "the correlation matrix, done"; if the team wants the Pearson `SERIES` version
  back, that requires tagging cell 66 and revising
  [03.3-univariate-and-time-structure.md](03.3-univariate-and-time-structure.md), not adding it
  here.
- **No hardcoded snapshot.** The record's extent may grow (see
  [03.1-setup.md](03.1-setup.md)); the final self-check must not repeat
  `EDA-simple-claude.ipynb` cell 84's specific row-count/date assertions, only the
  extent-independent structural ones.
- **Load-duration curve's full-record window.** `EDA-magc.ipynb`'s original defaulted to one
  calendar year; running it over the full multi-year record changes what "% of hours" means —
  state that explicitly in the accompanying prose so the reader does not assume the curve
  represents a single year.

## Acceptance criteria

- [ ] All four context plots (Behaviour 1–4) appear together as the notebook's clearly separated
      last content section, using dynamically computed years (no literal calendar year) and MWh
      / average MW units throughout.
- [ ] The Findings section explicitly references each of
      [03.2](03.2-sanity-check.md)–[03.7](03.7-context-appendix.md) by topic.
- [ ] The final self-check asserts index sortedness/uniqueness, zero `NaN`s across `SERIES`,
      `time_series` columns == `SERIES + DERIVED`, and no mutation since `LOADED` — and does
      **not** assert a hardcoded row count or date bound.
- [ ] No plot from [03.2](03.2-sanity-check.md)–[03.6](03.6-temporal-dependence-and-extremes.md)
      is duplicated into this section.

## Out of bounds

- Any plot belonging to an earlier section — this spec only adds the four context plots, the
  Findings section, and the self-check.
- Restoring the excluded Pearson `SERIES` correlation heatmap.
- A hardcoded row-count or date-bound assertion in the self-check.
- Everything [03-combined-cherry-picked-eda.md](03-combined-cherry-picked-eda.md) rules out
  globally.
